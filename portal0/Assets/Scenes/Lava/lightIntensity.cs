using System;
using System.Collections;
using System.Collections.Generic;
using System.Security.Cryptography;
using UnityEngine;


public class lightIntensity : MonoBehaviour
{
   // Light[] mesLumieres;
   // float variation = 40f;
    // Start is called before the first frame update
    void Start()
    {
    //    mesLumieres = FindObjectsOfType<Light>();

        
    }

    // Update is called once per frame
    void Update()
    {
      //  if (variation > 50f)
       // {
       //     variation = 10f;
       // }
       // variation += 10f * Time.deltaTime;
       // foreach (Light lumiere in mesLumieres)
        //{
        //    lumiere.intensity = variation;
      //  }
    }
}
